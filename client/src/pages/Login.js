import { Fragment } from "react";
import LoginForm from "../components/auth/LoginForm";

const Login = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default Login;
